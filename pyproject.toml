[tool.poetry]
name = "mysite"
version = "0.1.0"
description = "Django application with caching and Docker deployment"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
django = "^4.2"
django-filter = "^23.2"
djangorestframework = "^3.14"
pillow = "^10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"